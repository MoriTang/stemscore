{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the StemFlow window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
